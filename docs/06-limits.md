# 06 — Limits

What this system does not do, stated on day one so it is never discovered
later. Every entry here exists because someone would otherwise claim the
opposite.

---

## 1. "Constant time" means one specific thing

**Per-operation cost is constant.** Locating a slice, reading a bar, reading a
bar's condition bits, testing one candidate against one bar, rejecting a
duplicate, appending a result — each is O(1) and each is measured by gate 8.

**Total work is not constant.** It scales with symbols × bars × candidates. It
has to; that is the shape of the problem. A design can only promise the first,
and any claim of the second is false.

Both sentences go in any external description. Neither alone is honest.

---

## 2. The vendor pull can never be O(1)

It is bounded by someone else's rate limit — 500 requests per minute on the
primary vendor, 5 per second on the secondary. No language changes that.

What is achievable, and what is claimed: the pull is **bounded** (a governor
that never exceeds the ceiling under any concurrency), **resumable** (a killed
pull restarts at the last committed window), and **honest** (progress reflects
committed work, not issued requests).

---

## 3. The browser needs a generated loader

A browser runs one of two things: script, or WebAssembly. Shipping
WebAssembly produces a small generated loader alongside it.

**Zero hand-written non-Rust source is achievable and enforced.** Zero
generated bytes is not. Claiming otherwise would be the first lie in this
repository, and the whole point of the extension gate is that the repository
does not tell that kind of story.

The generated loader is build output. It is not committed, it is not edited,
and it is regenerated on every build.

---

## 4. Large-scale numbers are extrapolations

The per-operation costs below were measured on the reference machine (14-core
M4 Pro, 48 GB). Anything expressed in billions of trials is those numbers
multiplied out.

| Operation | Measured | Source |
|---|---|---|
| Superset test | 0.192 ns per bar·mask | 94,000 bars × 10,000 masks |
| Trade walk | 1.28 ns per bar·mask | same harness |
| Frequent-count pass | 0.257 ns per bar·mask | same harness |
| Record width | 56 bytes | this format |

**A full production sweep has never been run.** Every wall-clock figure for a
complete sweep is a model, not an observation. That sentence stays here until
the day a real sweep completes and its wall time is recorded — at which point
it is replaced by the measurement, with the hardware named.

---

## 5. The combination count still explodes

At 74 conditions:

| k | C(74, k) |
|---:|---:|
| 4 | 1,150,626 |
| 5 | 16,108,764 |
| 6 | 185,250,786 |
| 7 | 1,799,579,064 |
| 8 | 15,071,474,661 |

Holding every candidate exactly, at 32 bytes each, reaches 6 GiB at k=6 and
**59.7 GiB at k=7** — past the reference machine's memory.

What the design does about it: it never enumerates those levels. Apriori
pruning means level k is generated only from level k−1's survivors, and the
ladder stops at extinction. If pruning leaves nothing at k=5, k=7 does not
exist as a set to be too large.

What the design does **not** do: make a fully-dense vocabulary tractable. If
every condition were frequent and independent, the ladder would run to
exhaustion and the memory bound would bite. That would be a data problem
surfacing as a resource refusal, and the refusal is loud rather than an OOM.

---

## 6. Memory mapping has an uninterruptible failure

A page fault on a network mount that has become unreachable blocks in the
kernel and cannot be interrupted. No timeout, no signal handler, no cancel
token reaches it.

**Position:** keep the store on local disk. This is an operational rule, not
something the architecture fixes, and pretending otherwise would repeat
exactly the mistake the read-only-mapping decision was made to avoid.

---

## 7. What a green build does not prove

| Green gate | Does not mean |
|---|---|
| Extension allowlist | the design is good — only that it is one language |
| 100% coverage | the tests assert anything useful; that is what mutation testing is for |
| Gate 8 ratios | absolute speed is acceptable — only that it did not degrade with input size |
| `cargo deny` | a dependency is trustworthy — only that it is licensed and un-advised |
| Property tests | the property is the right one |
| A green build before `crates/core` exists | that anything compiled. Cargo refuses a virtual workspace with no members, so the toolchain gates are skipped and coverage is vacuously 100%. Both emit a CI warning saying so. The condition disarms permanently when the first crate lands, and it is checked against the tracked tree so an untracked directory cannot satisfy it. |

---

## 8. Open questions, carried forward

| Question | Status |
|---|---|
| Secondary-vendor index security ids other than NIFTY | community sources only, unverified — cross-check against the vendor scrip master before use |
| Secondary-vendor India VIX candle availability | undocumented — treat as a hard gate |
| Primary-vendor per-second request cap | undocumented; the production ceiling is a chosen value, not a measured one |
| Whether a bar timestamp marks the open or the close of its minute | assumed open; unverified against either vendor |

Each of these is a probe someone must run against a live credential. Until
then they stay in this table and out of any claim.
