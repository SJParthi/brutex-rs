# Pinned, checked in, identical on every machine and in CI.
# Bumping this is a decision — add an entry to docs/05-decisions.md.
[toolchain]
channel    = "1.85.0"
components = ["rustfmt", "clippy", "llvm-tools-preview"]
targets    = ["wasm32-unknown-unknown"]
profile    = "minimal"
